const _eepList = [];

_eepList['D2-01-09'] = require('./eep/D2-01-09');

let _rawUserData = null;

class VLD {
    constructor(rawUserData) {
        _rawUserData = rawUserData;
    }

    parse(eep) {
        if (eep) {

        } else {
            
        }
    }
}